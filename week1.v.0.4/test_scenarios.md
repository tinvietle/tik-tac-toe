| ID | Title | Req | Type | Steps | Expected | Priority | Notes/Comments |
|----|-------|-----|------|-------|----------|----------|----------------|
| TS-001 | Start game with human first | Start arg=1, greeting, initial board, first-turn message | Functional - Positive | 1. Run `java <main-class> 1`.<br>2. Observe startup output. | Terminal shows `Hello!`.<br>Next line shows initial 3x3 board with all cells `0`.<br>Next line shows `Player#1’s turn`. | P1 | Baseline happy path for startup flow. |
| TS-002 | Start game with computer first | Start arg=2, first-turn message | Functional - Positive | 1. Run `java <main-class> 2`.<br>2. Observe startup output. | Terminal shows `Hello!`, initial board of all `0`, then `Player#2’s turn`. | P1 | Confirms argument-driven first player selection. |
| TS-003 | Reject missing startup argument | Invalid startup argument handling | Functional - Negative | 1. Run `java <main-class>` (no args). | Terminal displays exactly `Please, input a valid option [1-2]` and program exits/does not start game loop. | P1 | Covers missing argument edge. |
| TS-004 | Reject invalid startup argument value | Invalid startup argument handling | Functional - Negative | 1. Run `java <main-class> 3`.<br>2. Repeat with `0`, `-1`, `abc`. | For each run, terminal displays exactly `Please, input a valid option [1-2]` and game does not proceed. | P1 | Batch negative checks for out-of-range/non-numeric args. |
| TS-005 | Validate startup argument strictness | "exactly either 1 or 2" | Functional - Edge | 1. Run `java <main-class> 01`.<br>2. Run `java <main-class> 1 2` (extra arg).<br>3. Run with quoted whitespace value `" 1"` if shell allows. | Input is rejected unless parser treats value as exact valid option by spec interpretation; if accepted, record as ambiguity/defect against "exactly" wording. | P2 | Requirement is ambiguous on formatting and extra args. Capture behavior and align with product owner. |
| TS-006 | Board renders as 3x3 with state values only | Board display rules, no reference numbering | Functional - Positive | 1. Start game with `1`.<br>2. Inspect initial board rendering.<br>3. Enter one valid move (e.g., `5`) and inspect board again. | Board is always 3x3 and displays only state values (`0`,`1`,`2`), not reference labels `1..9` as headers/placeholders. | P1 | Format chars (`|`, spacing) are not strictly specified. |
| TS-007 | Accept valid human move and update board | Human valid move flow | Functional - Positive | 1. Start game with `1`.<br>2. At `Player#1’s turn`, input `5`.<br>3. Observe board after move. | Cell 5 changes from `0` to `1`, board is displayed, then game proceeds to computer turn unless terminal state is win/draw. | P1 | Core move placement behavior. |
| TS-008 | Handle non-integer input as invalid | Human invalid non-integer input | Functional - Negative | 1. Start game with `1`.<br>2. Input `abc`.<br>3. Input `@`.<br>4. Input empty line. | For each invalid non-integer value (except `q`), terminal shows `Please, input a valid number [1-9]` then next line `Player#1’s turn`; game waits for next input. | P1 | Empty-line handling should be clarified if scanner trims input. |
| TS-009 | Quit game with q | Human `q` quit case | Functional - Positive | 1. Start game with `1`.<br>2. At human turn, input `q`. | Terminal shows `End of the game`; program stops immediately. | P1 | Verify exact lowercase `q` behavior. |
| TS-010 | Verify q case sensitivity | Human non-integer branch strictness | Functional - Edge | 1. Start game with `1`.<br>2. Input `Q`.<br>3. Input ` q` (leading space).<br>4. Input `q ` (trailing space). | Values not exactly `q` should be treated as invalid number input with message `Please, input a valid number [1-9]` then `Player#1’s turn`. | P2 | Confirms exact-string comparison. |
| TS-011 | Reject integer outside 1-9 | Human invalid integer range | Functional - Negative | 1. Start game with `1`.<br>2. Input `0`, then `10`, then `-3`. | Each out-of-range integer triggers `Please, input a valid number [1-9]` and next line `Player#1’s turn`; no board state change. | P1 | Ensure invalid attempts do not consume turn. |
| TS-012 | Reject move to occupied cell | Occupied cell validation | Functional - Negative | 1. Start game with `1`.<br>2. Input `1` (valid).<br>3. Wait for computer move.<br>4. On next human turn, input `1` again. | Terminal shows `The cell is occupied!` then next line `Player#1’s turn`; game waits for another human input. | P1 | Must preserve board state and current turn. |
| TS-013 | Human win detection on row/column/diagonal | Human win condition | Functional - Positive | 1. Execute a deterministic sequence where human completes a line (use computer-first-available behavior to plan moves).<br>2. Reach winning human move. | After winning move, current board is displayed, next line `Player#1 won!`, program ends. | P1 | Run at least 3 subcases: row, column, diagonal. |
| TS-014 | Computer win detection | Computer win condition | Functional - Positive | 1. Start game and input moves that allow computer to complete a line first.<br>2. Observe terminal when computer wins. | After computer winning move, board is displayed, next line `Player#2 won!`, program ends. | P1 | Must trigger immediately after computer move. |
| TS-015 | Draw detection when board full after human move | Draw condition after human move | Functional - Positive | 1. Play sequence so last free cell is filled by human without any winner.<br>2. Observe terminal. | Board is displayed then `It is a draw!`; program ends. | P1 | Validates branch after human move. |
| TS-016 | Draw detection when board full after computer move | Draw condition after computer move | Functional - Positive | 1. Play sequence so last free cell is filled by computer without any winner.<br>2. Observe terminal. | Board is displayed then `It is a draw!`; program ends. | P1 | Validates branch after computer move. |
| TS-017 | Computer chooses first available cell | Computer strategy | Functional - Positive | 1. Start with `2` so computer moves first.<br>2. Confirm first computer move is cell 1.<br>3. Make human move to occupy cell 2 or 3 as needed.<br>4. Confirm each computer turn picks smallest-index free cell. | Computer always places `2` at the lowest-numbered unoccupied cell from 1 to 9. | P1 | Deterministic AI behavior, easy to assert. |
| TS-018 | Board integrity after every move | Board reflects current state | Functional - Positive | 1. Play 5-6 turns with known inputs.<br>2. After each displayed board, track expected occupancy map.<br>3. Compare printed board each turn. | Every board display matches cumulative moves: human cells as `1`, computer cells as `2`, unoccupied as `0`; no unexpected overwrites. | P1 | Can be automated with stdout snapshot comparison. |
| TS-019 | Turn prompt sequence correctness | Turn messaging and flow continuity | Functional - Positive | 1. Start with arg `1` and play valid/invalid inputs mix (`abc`, valid move, occupied move, valid move).<br>2. Verify prompt shown after each branch. | Prompt text appears at correct times and with correct player number according to flow; invalid human inputs always return to `Player#1’s turn`. | P2 | Requirement always names `Player#1` for invalid human input; likely intentional due to fixed roles. |
| TS-020 | Program termination behavior on final states | End states stop program | Non-Functional - Reliability | 1. Execute one run ending in human win.<br>2. One run ending in computer win.<br>3. One run ending in draw.<br>4. One run with `q`. | In all end states, process exits cleanly with no extra prompt/read attempts and no stack traces. | P1 | Critical stability check. |
| TS-021 | Input robustness under rapid invalid retries | Input loop resilience | Non-Functional - Robustness | 1. Start game with `1`.<br>2. Enter 30+ invalid inputs quickly (`x`, `!`, `999`, blank).<br>3. Enter valid move. | Program remains responsive, does not crash, and still accepts valid move afterward. | P2 | Good guard against parser-loop defects. |
| TS-022 | Output consistency with exact required strings | Message contract verification | Non-Functional - Compatibility | 1. Capture terminal output for key branches (invalid start arg, invalid move, occupied cell, quit, win/draw).<br>2. Compare against requirement strings character-by-character. | Messages match required literals (including punctuation/brackets) or deviations are logged as defects. | P1 | Important if auto-grader checks exact output. |
| TS-023 | Requirement gap: OOP compliance is not black-box testable | OOP requirement | Gap/Untestable | 1. Perform code review against design checklist (classes, encapsulation, responsibilities).<br>2. Cross-check against architecture rubric. | Evidence of OOP principles is documented, but cannot be proven from terminal behavior alone. | P2 | Requires white-box review criteria; add explicit acceptance rubric. |
| TS-024 | Requirement gap: board visual format ambiguity | Board display format | Gap/Ambiguity | 1. Review sample/output expectation with stakeholders.<br>2. Decide exact board formatting (separators, spacing, line breaks). | Approved canonical format exists for test oracle and automated assertions. | P3 | Current spec defines content values, not strict rendering template. |
| TS-025 | Requirement gap: startup argument count ambiguity | Startup argument contract | Gap/Ambiguity | 1. Clarify whether extra CLI args are allowed when first arg is valid.<br>2. Update requirement/test oracle accordingly. | Clear rule documented: either strict single arg only, or first-arg-only parsing accepted. | P3 | Prevents conflicting implementations and flaky tests. |
| TS-026 | Requirement gap: non-integer parsing normalization | Human input strictness | Gap/Ambiguity | 1. Clarify whether inputs are trimmed before validation (` q`, `q `, ` 5`).<br>2. Define expected behavior and update tests. | Canonical parsing rule documented and reflected in automated tests. | P3 | Needed for consistent handling across scanner implementations. |
